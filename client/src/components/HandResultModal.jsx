import React from 'react';

// Settlement now renders through the non-blocking overlay and hand-history drawer.
// Keep this shell temporarily so existing imports remain stable during the UI transition.
const HandResultModal = () => null;

export default HandResultModal;
